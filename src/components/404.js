const NotFound = () => (
  <section className="MainSection">
    <div className="NothingFound" style={{ padding: '1rem' }}>
      Nothing found here
    </div>
  </section>
);

export default NotFound;
